{
  "extends": "./tsconfig.json",
  "exclude": ["node_modules", "test", "dist", "migrations", "private", "./jest.*", "**/*.spec.ts", "./pg-migrate.config.ts"],
  "watchOptions": {
    "watchFile": "useFsEvents"
  }
}